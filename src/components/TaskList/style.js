import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  noItemsText: {
    textAlign: "center",
    fontSize: 22,
    marginTop: 50,
  },
  listContainer: {
    marginHorizontal: 20,
    marginTop: 40,
  },
});
